{% extends 'base.html.twig' %}

{% block title %}
	{{ article.title }}
{% endblock %}

{% block body %}
	<h1>{{ article.title }}</h1>

	<div>
		{{ article.content }}
	</div>
	<a href="{{ path('home') }}">Retour à la page d'accueil</a>
{% endblock %}
