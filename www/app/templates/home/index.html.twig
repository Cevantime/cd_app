{% extends 'base.html.twig' %}

{% block title %}Accueil
{% endblock %}

{% block body %}
	<div class="example-wrapper">
		<h1>Bienvenue sur la page d'accueil</h1>

		<p>Vous pouvez dès maintenant accéder à une super liste d'article :</p>
		<ul>
			{% for a in articles %}
				<li>
					<a href="{{ path('article', { id: a.id }) }}">{{ a.title }}</a>
					par
					{{ a.user.username }}
				</li>
			{% endfor %}
		</ul>
	</div>
{% endblock %}
